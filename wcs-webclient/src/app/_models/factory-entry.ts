import {ImageEntry} from "./image-entry";

export class FactoryEntry {
  id?: number;
  title?: string;
  alias?: string;
  image?: ImageEntry;
  popular?: boolean;
  link?: string;
  imageInput?: any;
  description?: string;
}
